/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
/*
package db;
import java.sql.Connection;
import javax.swing.SwingUtilities;
import UI.MainFrame;
*/
/**
 *
 * @author danie
 */
/* public class TestConnection {
    public static void main(String[] args) {
        try (Connection con = AdminFirebird.getConnection()) {
            if (con != null && !con.isClosed()) {
                System.out.println("✅ Connectado a firebird");
                SwingUtilities.invokeLater(new Runnable() {
                    public void run() {
                        MainFrame app = new MainFrame(); // Instantiate the class
                        app.showGui(); // Call a method to display the GUI
                    }
                });
            }
        } catch (Exception e) {
            System.out.println("❌ Conection fallida");
            e.printStackTrace();
        }
    }
}
*/